language: d

os:
  - linux
  - mac

d:
  - dmd
  - gdc
  - ldc

install:

script:
  - dub test

branches:
  only:
    - master

notifications:
  email: true

sudo: false

matrix:
  allow_failures:
    - d: ldc
